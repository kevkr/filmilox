{
    "name": "filmilox",
    "version": "1.0.0",
    "main": "server.js",
    "license": "MIT",
    "private": true,
    "scripts": {
        "test": "NODE_ENV=test jest --coverage",
        "dev": "NODE_ENV=development nodemon --legacy-watch server.js",
        "docker-install": "docker exec filmilox-server yarn install",
        "prod": "NODE_ENV=production node server.js"
    },
    "dependencies": {
        "bcryptjs": "^2.4.3",
        "body-parser": "^1.20.0",
        "cors": "^2.8.5",
        "dotenv": "^16.0.1",
        "express": "^4.18.1",
        "express-validator": "^6.14.0",
        "jsonwebtoken": "^8.5.1",
        "mongoose": "^6.3.3",
        "multer": "^1.4.5-lts.1"
    },
    "devDependencies": {
        "jest": "^28.1.1",
        "mongodb-memory-server": "^8.6.0",
        "nodemon": "^2.0.16",
        "supertest": "^6.2.3"
    }
}
